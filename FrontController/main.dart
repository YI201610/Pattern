import 'FrontHandler.dart';

main() {
  var front_controller = new FrontController();

  Uri sampleUri = Uri("https://sample-domain.com", "userInfo", )
  front_controller.analyze(url: )

  print(front_controller);

}